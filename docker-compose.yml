services:
  mimo_desktop_bridge:
    build: .
    container_name: mimo_desktop_bridge
    ports:
      - "8787:8787"
    volumes:
      - mdb-data:/data
    restart: unless-stopped

volumes:
  mdb-data:
